server:
  port: 8081

spring:
  application:
    name: itinerary-service

  cache:
    type: redis

  data:
    redis:
      host: localhost
      port: 6379

management:
  endpoints:
    web:
      exposure:
        include: health,info